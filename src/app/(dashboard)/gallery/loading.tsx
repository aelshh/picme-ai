import React from "react";

const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen w-full z-100">
      Loading...
    </div>
  );
};

export default Loading;
